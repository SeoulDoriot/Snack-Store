# University Store

TypeScript monorepo scaffold for a QR-accessible university snack store.

- `frontend/`: Next.js customer and admin UI
- `backend/`: Express REST API
- `database/`: SQL schema, migrations, and seeds
- `scripts/`: operational utilities such as QR generation

Copy `.env.example` to `.env`, install dependencies, and run `npm run dev`.
